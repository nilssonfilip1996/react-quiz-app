export default function Header({title}) {
    return (
        <>
            <header>
                <img src="/quiz-logo.png" alt="" />
                <h1>{title}</h1>
            </header>
        </>
    );
}